/*import express from "express";

const app = express();
const port = 3000;

// Đường dẫn chính
app.get("/", (_req, res) => {
  res.send("Xin chào từ web Node.js!");
});

// Khởi chạy server
app.listen(port, () => {
  console.log(`Server đang chạy tại http://localhost:${port}`);
});*/
